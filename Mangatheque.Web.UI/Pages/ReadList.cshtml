@page
@model Mangatheque.Web.UI.Pages.ReadListModel
@using Mangatheque.Core.Infrastructure.Databases;
@using Microsoft.AspNetCore.Identity
@inject UserManager<MangathequeUser> UserManager
@{
    var user = await UserManager.GetUserAsync(User);
}



<h1>Mes mangas à lire</h1>
<br/><br/>

@if(user != null)
{
    <h2>@user.Email @user.Id</h2>
}